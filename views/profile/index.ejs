<html>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.8.0/Chart.min.css">
<body>
<h1><%= currentUser.firstname %>'s Profile Page</h1>

<div class="container">
  <div class="left">
    <img src="<%= currentUser.profile %>" alt="user profile pic">
    <p><strong>Name:</strong> <%= currentUser.firstname %> <%= currentUser.lastname %></p>
    <p><strong>Admin?</strong> <%= currentUser.admin ? 'Yes' : 'No' %></p>
  </div>
  <div class="right">
    <p>
      <%= currentUser.firstname %> was born on <%= moment(currentUser.birthday).format('MMM do YYYY') %> and uses the
      display name <%= currentUser.username %>. You can email <%= currentUser.firstname %> at <%= currentUser.email %>!
    </p>
    <hr>
    <h2><%= currentUser.firstname %>'s stock platform!</h2>
    <p><%= currentUser.bio %></p>
    <hr>
   
<a href="./stock">See All Favorites</a>

<canvas id="canvas" width="400" height="400">
    HELLLOOOOOOOOOOOOOOOO
</canvas>

<% stocks.forEach(stocks => { %>
    
  <li >
    
      Date: <p id="Date"> <%= stocks.date.slice(0, 10) %> </p>
      <hr>
      Ticker:<p id="Symbol"> <%= stocks.symbol %> </p>
      <hr>
      
      <hr>
      Close:<p id="Close"> <%= stocks.close %> </p>
      <hr>
      
      <hr>

  </li>
  <% }) %>


    <form method="POST" action="/search">
      <!-- <input  type="text" name="ticker" onChange="tickSearch()"> -->
      

<hr>
  <input id="input" list="list" name="ticker" value="">
      <datalist id="list" >
        <option id="option" value=""></option>
      </datalist>
      

      <input type="submit">


    </form>

   



  </div>
</div>

<script src="./app.js"></script>
<script  src="./chart.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.8.0/Chart.min.js"></SCript>
</body>
</html>